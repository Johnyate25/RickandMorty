// script.js

// Mostrar y ocultar splash screen
window.addEventListener('load', () => {
    const splash = document.getElementById('splash');
    setTimeout(() => {
      splash.style.display = 'none';
    }, 2000);
  });
  
  // Aquí iría la conexión con la API de Rick and Morty
  // Ejemplo: https://rickandmortyapi.com/api/character
  
  // Funcionalidad del buscador y filtro
  const searchInput = document.getElementById('searchInput');
  const filterSelect = document.getElementById('filterSelect');
  const charactersList = document.getElementById('charactersList');
  
  // Aquí se colocará la lógica para obtener y mostrar personajes según búsqueda y filtro
  
  // CRUD de favoritos con localStorage
  // Aquí implementas las funciones para guardar, mostrar, actualizar y eliminar favoritos
  
  // Funcionalidad original (puedes definir algo creativo aquí, como “mezclar personajes”)
  
  